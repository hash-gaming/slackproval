<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <meta name="description" content="">
    <meta name="author" content="">
    <link rel="icon" href="../../favicon.ico">

    <title>Slackproval</title>
    <%= csrf_meta_tags %>

    <link rel="stylesheet" href="https://cdn.jsdelivr.net/simplemde/latest/simplemde.min.css">
    <script src="https://cdn.jsdelivr.net/simplemde/latest/simplemde.min.js"></script>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body class="flex-container flex-column">
    <nav class="navbar navbar-dark bg-dark flex-item-static">
      <div class="container d-flex justify-content-between">
        <%= link_to ENV["SLACK_NAME"], root_path, class: "navbar-brand" %>
        <form class="form-inline my-2 my-lg-0">
          <% if current_user %>
            <div class="btn-group" role="group" aria-label="Basic example">
              <%= link_to requests_path, class: "btn btn-outline-success btn-sm" do %>
                Requests <span class="badge badge-light"><%= Request.new_items.count if Request.new_items.any? %></span>
              <% end %>
              <%= link_to fa_icon("gavel"), bans_path, class: "btn btn-outline-light btn-sm" %>
              <% if ENV["CODE_OF_CONDUCT_REQUIRED"] == 'true'%>
                <%= link_to fa_icon("book"), code_of_conduct_path, class: "btn btn-outline-light btn-sm" %>
              <% end %>
              <%= link_to fa_icon("users"), users_path, class: "btn btn-outline-light btn-sm" %>
              <%= link_to fa_icon("sign-out"), :logout, method: :post, class: "btn btn-outline-light btn-sm" %>
            </div>
          <% end %>
        </form>
      </div>
    </nav>
    <div class="site-content flex-item-dynamic">
      <%= yield %>
    </div>

    <div class="flex-item-static">
      <%= yield :footer %>
    </div>
  </body>
</html>
